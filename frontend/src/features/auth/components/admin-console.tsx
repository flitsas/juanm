"use client";

import { UnifiedAdminPage } from "./unified-admin-page";

/** @deprecated Usar UnifiedAdminPage en /admin dentro del shell. */
export function AdminConsole() {
  return <UnifiedAdminPage />;
}
